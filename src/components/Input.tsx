import styled from "styled-components";

export const Input = styled.input`
    padding: 15px;
    outline: none;
    background-color: var(--BackGray);
    border: none;
    border-radius: 5px;
    color: var(--FontGray);
`;
